import React from 'react';
import ReactDOM from 'react-dom/client';
import EGBudgetApp from './eg-budget-app';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <EGBudgetApp />
  </React.StrictMode>
);
